splash = false

# hyprpaper 0.8+ uses wallpaper blocks. The session helper rasterizes the
# generated Tahoe-style PNG directly. Keep the monitor selector empty so the
# same fallback reaches all outputs.
wallpaper {
    monitor =
    path = ~/.config/hypr/wallpapers/tahoe.png
    fit_mode = cover
}
