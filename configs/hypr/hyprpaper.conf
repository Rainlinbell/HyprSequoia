splash = false

# hyprpaper 0.8+ uses wallpaper blocks. The session helper rasterizes the
# project SVG into this cache path because not every hyprpaper build supports
# SVG. Keep the monitor selector empty so the same fallback reaches all outputs.
wallpaper {
    monitor =
    path = ~/.cache/hyprsequoia/default.png
    fit_mode = cover
}
